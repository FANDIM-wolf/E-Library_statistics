using Avalonia.Controls;

namespace ELibraryStatistics.Views {
  // The main window. All behaviour lives in MainWindowViewModel; this class is
  // only the XAML host.
  public partial class MainWindow : Window {
    public MainWindow() {
      InitializeComponent();
    }
  }
}
